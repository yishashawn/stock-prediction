# 模型功能增强说明

本文档说明已实现的所有模型增强功能。

## ✅ 已实现功能

### 1. 高级NLP模型 ✅

**功能描述**：
- 使用BERT等预训练模型进行新闻情绪分析
- 支持分析新闻正文内容，而不仅仅是标题
- 自动回退到关键词匹配（如果BERT不可用）

**实现位置**：
- `predict_stock_price_advanced.py` 中的 `get_news_sentiment()` 函数
- 支持参数 `use_advanced_nlp=True` 启用BERT分析

**使用方法**：
```python
# 使用BERT分析新闻情绪
news_sentiment_df = get_news_sentiment(ts_code, start_date, end_date, use_advanced_nlp=True)
```

**依赖**：
- `transformers` 库
- `torch` 库

**安装命令**：
```bash
pip install transformers torch
```

---

### 2. 更多宏观经济指标 ✅

**功能描述**：
- 新增利率数据（SHIBOR隔夜利率）
- 新增汇率数据（USDCNY美元对人民币汇率）
- 新增M2货币供应量数据

**实现位置**：
- `predict_stock_price_advanced.py` 中的 `get_macro_economic_data()` 函数

**新增指标**：
- `interest_rate`: 利率
- `interest_rate_change`: 利率变化
- `exchange_rate`: 汇率
- `exchange_rate_change`: 汇率变化率
- `m2_yoy`: M2同比增长率
- `m2_mom`: M2环比增长率

**注意**：这些指标需要相应的Tushare权限。

---

### 3. 技术指标扩展 ✅

**功能描述**：
- 新增KDJ指标（随机指标）
- 新增BOLL指标（布林带）
- 新增CCI指标（商品通道指数）
- 新增OBV指标（能量潮）
- 新增WR指标（威廉指标）
- 新增DMI指标（趋向指标）

**实现位置**：
- `predict_stock_price_advanced.py` 中的 `calculate_technical_indicators()` 函数

**新增指标**：
- `kdj_k`, `kdj_d`, `kdj_j`: KDJ指标
- `boll_mid`, `boll_upper`, `boll_lower`, `boll_width`, `boll_position`: 布林带指标
- `cci`: CCI指标
- `obv`, `obv_ma`, `obv_ratio`: OBV指标
- `wr`: 威廉指标
- `plus_di`, `minus_di`, `adx`: DMI指标

**特征交互项**：
- 已实现特征交互项（技术指标 × 外部因素）
- 在 `enhance_features()` 函数中实现
- 自动限制交互项数量，避免特征爆炸

---

### 4. 模型解释性 ✅

**功能描述**：
- SHAP值分析（解释每个特征的贡献）
- LIME局部解释（解释单个预测）
- 特征重要性可视化（多种方法对比）

**实现位置**：
- `model_enhancements.py` 中的相关函数

**主要函数**：
- `calculate_shap_values()`: 计算SHAP值
- `calculate_lime_explanation()`: 计算LIME解释
- `visualize_feature_importance()`: 可视化特征重要性

**输出文件**：
- `中际旭创_SHAP特征重要性.png`: SHAP摘要图
- `中际旭创_LIME解释.html`: LIME解释网页
- `中际旭创_特征重要性详细分析.png`: 特征重要性对比图

**依赖**：
- `shap` 库
- `lime` 库

**安装命令**：
```bash
pip install shap lime
```

---

### 5. 风险管理 ✅

**功能描述**：
- 预测区间（置信区间）
- 风险指标计算（波动率、最大回撤、VaR、CVaR、夏普比率）
- 止损/止盈建议生成

**实现位置**：
- `model_enhancements.py` 中的相关函数

**主要函数**：
- `calculate_prediction_intervals()`: 计算预测区间
- `calculate_risk_metrics()`: 计算风险指标
- `generate_stop_loss_take_profit()`: 生成止损止盈建议

**风险指标**：
- `volatility`: 波动率
- `max_drawdown`: 最大回撤
- `var_95`: 95%风险价值
- `cvar_95`: 95%条件风险价值
- `sharpe_ratio`: 夏普比率

---

### 6. 自动化部署 ✅

**功能描述**：
- 模型版本管理（保存、加载、列出版本）
- 定时任务设置（每天自动更新）
- A/B测试不同模型版本

**实现位置**：
- `model_enhancements.py` 中的相关类和函数

**主要功能**：
- `ModelVersionManager`: 模型版本管理器
- `setup_auto_update_task()`: 设置自动更新任务
- `ab_test_models()`: A/B测试模型

**使用方法**：
```python
from model_enhancements import ModelVersionManager

# 保存模型版本
version_manager = ModelVersionManager()
version_manager.save_model(model, version=1, metadata={'r2': 0.65})

# 加载模型版本
model = version_manager.load_model(version=1)

# 列出所有版本
versions = version_manager.list_versions()
```

**依赖**：
- `schedule` 库（用于定时任务）

**安装命令**：
```bash
pip install schedule
```

---

## 🚧 待实现功能

### 7. 模型架构优化（Transformer、图神经网络、强化学习）

**计划**：
- Transformer模型（更适合时间序列）
- 图神经网络（捕捉股票间关系）
- 强化学习（用于交易策略优化）

**状态**：待实现

---

### 8. 实时数据集成

**计划**：
- 实时新闻流分析
- 社交媒体情绪（微博、股吧等）
- 实时资金流向数据

**状态**：部分实现（框架已搭建，需要API接口）

**实现位置**：
- `model_enhancements.py` 中的 `get_realtime_news_sentiment()`, `get_social_media_sentiment()`, `get_realtime_money_flow()` 函数

**注意**：这些功能需要相应的API接口支持。

---

## 📝 使用说明

### 集成增强功能

在主脚本运行后，会自动集成增强功能：

```python
# 在主脚本中已自动调用
integrate_enhancements(model, X_test, y_test, feature_cols, merged, predictions)
```

### 手动调用增强功能

```python
from model_enhancements import (
    calculate_shap_values,
    calculate_risk_metrics,
    ModelVersionManager
)

# SHAP分析
shap_values, importance_df, explainer = calculate_shap_values(
    model, X_sample, feature_names
)

# 风险指标
risk_metrics = calculate_risk_metrics(predictions, actuals, prices)

# 模型版本管理
version_manager = ModelVersionManager()
version_manager.save_model(model, version=1)
```

---

## 📦 依赖安装

所有新增功能的依赖已添加到 `requirements.txt`：

```bash
pip install -r requirements.txt
```

主要新增依赖：
- `transformers>=4.20.0` (BERT模型)
- `torch>=1.12.0` (PyTorch)
- `shap>=0.41.0` (SHAP值分析)
- `lime>=0.2.0` (LIME解释)
- `schedule>=1.2.0` (定时任务)

---

## 📊 输出文件

增强功能会生成以下文件：

1. **模型解释性**：
   - `中际旭创_SHAP特征重要性.png`
   - `中际旭创_LIME解释.html`
   - `中际旭创_特征重要性详细分析.png`

2. **风险管理**：
   - 风险指标在控制台输出
   - 止损止盈建议在控制台输出

3. **模型版本**：
   - `models/v{version}/model.pkl` (模型文件)
   - `models/v{version}/metadata.json` (元数据)

4. **A/B测试**：
   - `中际旭创_AB测试结果.png`

---

## ⚠️ 注意事项

1. **BERT模型**：首次使用需要下载预训练模型，可能需要一些时间。
2. **SHAP/LIME**：计算可能需要较长时间，建议使用样本数据。
3. **实时数据**：需要相应的API接口和权限。
4. **模型版本**：模型文件可能较大，注意磁盘空间。

---

**最后更新**：2025年1月


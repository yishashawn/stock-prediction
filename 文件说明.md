# 中际旭创股票价格预测系统 - 文件说明

## 核心文件

### 1. 预测脚本
- **`predict_stock_price_advanced.py`** - 主预测脚本
  - 功能：预测中际旭创未来5个工作日的股价
  - 使用方法：`python predict_stock_price_advanced.py`
  - 生成文件：
    - `中际旭创_价格预测.html` - 网页表格（包含预测结果和图表）
    - `中际旭创_价格预测数据.csv` - 预测数据CSV文件
    - `中际旭创_价格预测模型.png` - 预测模型图表
    - `中际旭创_多因素散点图.png` - 多因素散点图分析
    - `prediction_history.json` - 预测历史记录

### 2. 更新脚本
- **`update_predictions.py`** - 每日更新脚本
  - 功能：每天收盘后运行，自动更新实际收盘价并重新训练模型
  - 使用方法：`python update_predictions.py`

### 3. 配置文件
- **`requirements.txt`** - Python依赖包列表
- **`prediction_history.json`** - 预测历史记录（自动生成）

### 4. 文档文件
- **`README.md`** - 项目说明文档
- **`模型说明文档.md`** - 详细的模型说明文档

## 生成的结果文件

运行预测脚本后会自动生成以下文件：

1. **`中际旭创_价格预测.html`** - 网页表格
   - 包含价格走势图（实际值 vs 预测值）
   - 未来5个工作日的预测数据
   - 历史预测记录

2. **`中际旭创_价格预测数据.csv`** - 预测数据
   - 包含日期、实际收盘价、预测收盘价、误差等

3. **`中际旭创_价格预测模型.png`** - 模型图表
   - 展示模型拟合效果

4. **`中际旭创_多因素散点图.png`** - 多因素分析
   - 展示各指标与股票涨跌幅的关系
   - 包含相关系数和趋势线

5. **`prediction_history.json`** - 预测历史
   - 记录所有历史预测和实际数据

## 使用方法

### 首次运行
```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 运行预测脚本
python predict_stock_price_advanced.py
```

### 每日更新
```bash
# 每天收盘后运行更新脚本
python update_predictions.py
```

## 模型特点

- **预测天数**：未来5个工作日
- **特征数量**：46个特征（技术指标、市场情绪、业绩指标、外部因素）
- **模型类型**：线性回归 / 随机森林（自动选择最优）
- **数据对齐**：所有特征使用前一天的数据进行预测

## 注意事项

- 所有生成的文件会在每次运行时自动更新
- `prediction_history.json` 会记录历史预测，用于模型改进
- 建议每天收盘后运行 `update_predictions.py` 更新数据


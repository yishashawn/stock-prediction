# 数据获取状态说明

本文档列出模型尝试获取的所有数据源，以及哪些数据可能无法获取。

## 数据获取状态分类

- ✅ **已获取**: 数据成功获取并用于模型
- ⚠️ **部分获取**: 数据部分可用，但可能不完整
- ❌ **无法获取**: 数据获取失败，通常需要更高权限或接口不可用
- 🔄 **可选**: 数据获取失败不影响模型运行

---

## 一、基础数据（必需）

### ✅ 中际旭创股票数据
- **接口**: `pro.daily()`
- **状态**: ✅ 必需，通常可获取
- **包含**: 开盘价、收盘价、最高价、最低价、成交量、涨跌幅等
- **说明**: 如果无法获取，程序无法运行

### ✅ 技术指标（自动计算）
- **来源**: 基于股票数据计算
- **状态**: ✅ 自动计算，无需外部数据
- **包含**: 
  - MA5/MA10/MA20均线
  - RSI、MACD
  - KDJ、BOLL、CCI、OBV、WR、DMI（新增）
  - 动量、波动率等
- **说明**: 所有技术指标都是基于股票数据自动计算的

---

## 二、市场指数数据

### ✅ 大盘指数（通常可获取）
- **接口**: `pro.index_daily()` 或 `pro.daily()`
- **状态**: ✅ 通常可获取
- **包含**:
  - 创业板指 (399006.SZ)
  - 深证成指 (399001.SZ)
  - 沪深300 (000300.SH)
  - 上证指数 (000001.SH)
  - 中证500 (000905.SH)
- **说明**: 这些是常用指数，通常有权限访问

### ❌ 行业指数（通常无法获取）
- **接口**: `pro.index_daily()` / `pro.daily()` / `ts.pro_bar()`
- **状态**: ❌ 通常无法获取，需要更高权限
- **尝试获取的指数**:
  - 半导体(申万) (801081.SI)
  - 通信(申万) (801770.SI)
  - 电子(申万) (801080.SI)
  - 光学光电子(申万) (801084.SI)
  - 通信设备(申万) (801102.SI)
- **说明**: 行业指数通常需要更高的Tushare积分或权限

### ⚠️ 同行业相关股票
- **接口**: `pro.daily()`
- **状态**: ⚠️ 部分可获取
- **尝试获取**:
  - 新易盛 (300394.SZ) - 光模块相关
- **说明**: 取决于股票代码是否正确和是否有权限

---

## 三、资金流向数据

### ⚠️ 资金流向
- **接口**: `pro.moneyflow()`
- **状态**: ⚠️ 需要相应权限
- **包含**: 
  - 资金净流入金额 (`net_mf_amount`)
  - 资金净流入比率 (`net_ratio`)
- **说明**: 需要Tushare相应权限，如果无法获取，程序会继续运行

---

## 四、基本面数据

### ⚠️ 股票基本面数据
- **接口**: `pro.daily_basic()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - 换手率 (`turnover_rate`)
  - 流通市值 (`circ_mv`)
  - 总市值 (`total_mv`)
- **说明**: 需要Tushare相应权限

---

## 五、市场情绪/认可度指标

### ⚠️ 融资融券数据
- **接口**: `pro.margin()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - 融资余额变化率 (`margin_balance_change`)
  - 融券余额变化率 (`short_balance_change`)
  - 融资占比 (`margin_ratio`)
- **说明**: 需要Tushare融资融券数据权限

### ⚠️ 股东户数数据
- **接口**: `pro.stk_holdernumber()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - 股东户数变化率 (`holder_change_pct`)
- **说明**: 需要Tushare相应权限，数据更新频率较低（通常按季度）

### ❌ 机构持仓数据
- **接口**: `pro.fund_hold()`
- **状态**: ❌ 通常无法获取
- **包含**:
  - 机构持仓量变化率 (`inst_hold_vol_change`)
  - 机构持仓数量变化率 (`inst_hold_num_change`)
- **说明**: 此接口经常返回"请指定正确的接口名"错误，可能需要更高权限或接口名称变更

---

## 六、公司业绩指标

### ⚠️ 财务指标数据
- **接口**: `pro.fina_indicator()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - 净资产收益率 (`roe`)
  - 总资产收益率 (`roa`)
  - 毛利率 (`grossprofit_rate`)
  - 净利率 (`netprofit_rate`)
  - 每股收益 (`eps`)
  - 每股净资产 (`bps`)
- **说明**: 需要Tushare财务数据权限，数据更新频率较低（通常按季度）

### ⚠️ 利润表数据
- **接口**: `pro.income()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - 营收同比增长率 (`revenue_yoy`)
  - 净利润同比增长率 (`n_income_yoy`)
  - 营收环比增长率 (`revenue_qoq`)
  - 净利润环比增长率 (`n_income_qoq`)
- **说明**: 需要Tushare财务数据权限，数据更新频率较低（通常按季度）

### ⚠️ 业绩预告数据
- **接口**: `pro.forecast()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - 业绩预告乐观度 (`forecast_optimism`)
- **说明**: 需要Tushare相应权限，数据更新频率较低（通常按季度或半年度）

---

## 七、宏观经济指标

### ⚠️ CPI数据（居民消费价格指数）
- **接口**: `pro.cn_cpi()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - CPI同比增长率 (`cpi_yoy`)
  - CPI环比增长率 (`cpi_mom`)
- **说明**: 需要Tushare宏观经济数据权限，数据为月度数据

### ⚠️ PMI数据（采购经理人指数）
- **接口**: `pro.cn_pmi()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - PMI指数 (`pmi`)
  - PMI变化 (`pmi_change`)
- **说明**: 需要Tushare宏观经济数据权限，数据为月度数据

### ⚠️ GDP数据（国内生产总值）
- **接口**: `pro.cn_gdp()`
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - GDP同比增长率 (`gdp_yoy`)
- **说明**: 需要Tushare宏观经济数据权限，数据为季度数据

### ❌ 利率数据
- **接口**: `pro.shibor()` (尝试使用SHIBOR作为利率指标)
- **状态**: ❌ 通常无法获取或接口不可用
- **包含**:
  - 利率 (`interest_rate`)
  - 利率变化 (`interest_rate_change`)
- **说明**: 可能需要更高权限或使用其他接口

### ❌ 汇率数据
- **接口**: `pro.fx_daily()` (美元对人民币汇率)
- **状态**: ❌ 通常无法获取或接口不可用
- **包含**:
  - 汇率 (`exchange_rate`)
  - 汇率变化率 (`exchange_rate_change`)
- **说明**: 可能需要更高权限或使用其他接口

### ❌ M2货币供应量
- **接口**: `pro.cn_m()`
- **状态**: ❌ 通常无法获取或接口不可用
- **包含**:
  - M2同比增长率 (`m2_yoy`)
  - M2环比增长率 (`m2_mom`)
- **说明**: 可能需要更高权限或使用其他接口

---

## 八、外部市场数据

### ✅ 比特币数据
- **接口**: `yfinance` (BTC-USD)
- **状态**: ✅ 通常可获取
- **包含**:
  - 比特币涨跌幅 (`btc_pct`)
- **说明**: 使用yfinance库，通常可正常获取

### ✅ 美股指数数据
- **接口**: `yfinance`
- **状态**: ✅ 通常可获取
- **包含**:
  - 标普500涨跌幅 (`SP500_pct`)
  - 纳斯达克涨跌幅 (`NASDAQ_pct`)
  - 道琼斯涨跌幅 (`DOW_pct`)
- **说明**: 使用yfinance库，通常可正常获取

---

## 九、新闻情绪数据

### ⚠️ 新闻情绪分析
- **接口**: `pro.news()` (Tushare新闻接口)
- **状态**: ⚠️ 需要相应权限
- **包含**:
  - 新闻情绪得分 (`news_sentiment`)
- **说明**: 
  - 如果无法获取Tushare新闻，会使用关键词匹配方法
  - 如果安装了transformers库，可以使用BERT模型进行更准确的情绪分析

---

## 十、时间序列特征

### ✅ 时间序列特征（自动生成）
- **来源**: 基于已有特征自动生成
- **状态**: ✅ 自动生成，无需外部数据
- **包含**:
  - 滞后特征（1天、2天、3天）
  - 滑动窗口统计（5日、10日均值和标准差）
- **说明**: 所有时间序列特征都是基于已有数据自动计算的

---

## 数据获取失败的影响

### 不影响模型运行的数据
以下数据获取失败不会影响模型运行，程序会继续使用其他可用数据：
- ❌ 行业指数
- ❌ 机构持仓数据
- ⚠️ 融资融券数据
- ⚠️ 股东户数数据
- ⚠️ 公司业绩指标（财务指标、利润表、业绩预告）
- ⚠️ 宏观经济指标（CPI、PMI、GDP、利率、汇率、M2）
- ⚠️ 新闻情绪数据
- ⚠️ 资金流向数据
- ⚠️ 基本面数据

### 必需的数据
以下数据如果无法获取，程序可能无法正常运行：
- ✅ 中际旭创股票数据（必需）
- ✅ 至少一个大盘指数数据（建议）

---

## 如何查看数据获取状态

运行程序时，控制台会显示每个数据源的获取状态：
- `✓ 成功获取...` - 数据获取成功
- `✗ 无法获取...` - 数据获取失败
- `警告: ...` - 数据部分可用或有问题

---

## 提升数据获取成功率的方法

1. **提升Tushare积分**: 在 [tushare.pro](https://tushare.pro) 提升积分等级
2. **申请相应权限**: 在Tushare网站申请所需的数据接口权限
3. **检查网络连接**: 确保网络连接正常，可以访问Tushare API
4. **检查API Token**: 确保Tushare Token有效且未过期

---

**最后更新**: 2025年1月


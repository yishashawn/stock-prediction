# 本地网站使用说明

## 🎯 两种方式访问网站

### 方法1: 直接打开HTML文件（最简单）

**操作**:
1. 双击运行: `直接打开网站.bat`
2. 网站会在默认浏览器中打开

**特点**:
- ✅ 最简单，无需任何配置
- ✅ 可以离线使用
- ❌ 只能在本机访问
- ❌ 无法分享给他人

### 方法2: 启动本地Web服务器（推荐）

**操作**:
1. 双击运行: `启动本地网站.bat`
2. 服务器启动后，会自动打开浏览器
3. 会显示访问地址，例如：
   - 本地访问: `http://localhost:8000/中际旭创_价格预测.html`
   - 局域网访问: `http://192.168.1.100:8000/中际旭创_价格预测.html`

**特点**:
- ✅ 可以在局域网内分享给他人
- ✅ 支持多设备访问
- ✅ 更接近真实网站体验
- ⚠️ 需要保持服务器运行

**停止服务器**: 在服务器窗口按 `Ctrl+C`

## 📋 使用步骤

### 步骤1: 生成网站文件

如果还没有HTML文件，先运行：
```
运行预测模型.bat
```

### 步骤2: 启动网站

**方式A - 直接打开**:
```
双击: 直接打开网站.bat
```

**方式B - Web服务器**:
```
双击: 启动本地网站.bat
```

### 步骤3: 访问网站

- **直接打开**: 网站会自动在浏览器中打开
- **Web服务器**: 
  - 本地访问: `http://localhost:8000/中际旭创_价格预测.html`
  - 局域网访问: 将显示的IP地址分享给同一网络内的其他人

## 🔄 更新网站

每次运行预测模型后：

1. **运行**: `运行预测模型.bat`（生成新的HTML）
2. **刷新浏览器**: 按 `F5` 或 `Ctrl+R` 刷新页面

## 📱 分享给他人（使用Web服务器）

1. **启动服务器**: 运行 `启动本地网站.bat`
2. **查看IP地址**: 服务器会显示局域网IP，例如 `http://192.168.1.100:8000`
3. **分享地址**: 将地址发送给同一WiFi/网络内的其他人
4. **他们访问**: 在浏览器中输入地址即可

**注意**:
- 其他人需要与您在同一个局域网（同一WiFi/网络）
- 确保防火墙允许端口8000的访问
- 服务器运行时，其他人才能访问

## 🌐 如果需要公网访问

如果需要在公网访问（不在同一网络也能访问），可以使用：

1. **GitHub Pages**（免费）: 推送到GitHub，自动生成公网地址
2. **Netlify**（免费）: 拖拽HTML文件即可
3. **其他静态网站托管服务**

详细步骤请查看: `README_网站部署说明.md`

## ❓ 常见问题

### Q: 直接打开HTML文件，图片无法显示？

**A**: 
- 确保PNG图片文件与HTML在同一目录
- 或使用Web服务器方式（推荐）

### Q: Web服务器无法启动？

**A**: 
- 检查Python是否已安装
- 检查端口8000是否被占用
- 尝试关闭防火墙

### Q: 其他人无法访问我的Web服务器？

**A**: 
- 确认在同一局域网
- 检查防火墙设置
- 确认服务器正在运行

---

**现在可以双击 `直接打开网站.bat` 或 `启动本地网站.bat` 访问网站！** 🎉


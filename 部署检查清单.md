# GitHub Pages 部署检查清单

## ✅ 当前状态

- ✓ Git已安装（版本 2.49.0）
- ✓ Git用户信息已配置
- ✓ 当前目录是Git仓库

## 📋 部署前检查

### 1. 检查文件

运行以下命令检查文件是否存在：
```bash
dir 中际旭创_*.html
dir 中际旭创_*.png
```

如果HTML文件不存在，请先运行：
```bash
运行预测模型.bat
```

### 2. 检查GitHub Token

- [ ] 已创建Personal Access Token
- [ ] Token权限包含 `repo`
- [ ] 已复制并保存Token

**创建Token**: https://github.com/settings/tokens

### 3. 检查GitHub仓库

- [ ] 已创建GitHub仓库
- [ ] 仓库设置为Public（公开）
- [ ] 已复制仓库URL

**创建仓库**: https://github.com/new

## 🚀 部署步骤

### 方法1: 使用批处理文件（推荐）

1. **双击运行**: `start_deploy.bat`
2. **按提示操作**:
   - 如果提示初始化仓库，输入 `y`
   - 输入GitHub仓库URL
   - 输入提交信息
   - 当提示输入密码时，粘贴Token

### 方法2: 手动部署

1. **打开PowerShell**（以管理员身份）
2. **运行**:
   ```powershell
   $env:Path += ";C:\Program Files\Git\bin;C:\Program Files\Git\cmd"
   cd C:\Users\syy\Tushare
   python "部署到GitHub_Pages.py"
   ```

## 📝 部署后操作

1. **启用GitHub Pages**:
   - 打开GitHub仓库页面
   - Settings → Pages
   - Source: 选择 `main` 和 `/ (root)`
   - 点击 Save

2. **访问网站**:
   - 等待1-2分钟
   - 访问: `https://您的用户名.github.io/仓库名/中际旭创_价格预测.html`

## ❓ 如果遇到问题

### 问题1: 提示"Git未在PATH中"

**解决**: 运行 `添加Git到PATH.bat`（以管理员身份运行），然后重启PowerShell

### 问题2: 提示"Authentication failed"

**解决**: 
- 确保使用Personal Access Token，不是GitHub密码
- 检查Token是否过期
- 确认Token有 `repo` 权限

### 问题3: 提示"文件不存在"

**解决**: 先运行 `运行预测模型.bat` 生成HTML文件

---

**现在可以运行 `start_deploy.bat` 开始部署！** 🎯

